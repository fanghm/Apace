TODO:
---------
Display action
  - Add display filters: category, status...
    eg: Filter: [select field] [=|<>|...] [select value]  // for date field, select date range to filter
  - Expand each item to display update history, and can add one more update from main UI conveniently
  - Load data on demand when switching tabs

Add action 
  - Data validation [x]
  - Read action category/status definitions from backend/database, and allow to update from setting
  - Datetime picker for LE input [x]
  - Input assistence for owner field: either auto-complete, or search
  - Verify owner is valid (for sending email notification)
  - Display update history in list [x]

Update/Remove action
  - Basic [x]
  - Store update history as array, and display as list [x]
  - Add new update with fixed prefix of date and author's name, like '[2016/09/09] frank: '
  (partly dependent on user authentication)

Keyboard shortcuts support
  - '/': search [x]
  - ctrl + 'a': add    [x]
  - ctrl + 'e': edit   [ ]
  - ctrl + 'd': delete [ ]
  - 'k/j' or up/down arrow: select prev/next item [ ]
  - 'n/p' or page up/down: page up/down           [ ]
  - For modal:                                    [x]
    - ctrl + 'd': delete
    - ctrl + 's': save
    - ESC: close modal

Login and permission control
  - LDAP authentication [x]
  - Store user info in session for personalized display (my actions, pagination setting, ...)
  - Permission control: 
    - admin can update global settings like category/status definitions, default pagination setting

Setting
  - category/status definitions
  - pagination setting
  - user default filter expression like "{category: 'CIF meeting', status: 'ongoing'}"

Others
  - app logo    [x]
  - favicon     [x]
  - send mail notification upon action creation, and when due date is less than N days (configurable)
  - Provide permlink of each action item (as mail link), can display/update separately (probably in a tab)
  - Action update logging/history for audit?
  - SaaS
  - handle shortcut routes with in-built filters, for quick access, like /cif -> {category: 'CIF meeting'}

Refactoring
  - put modal html into separate file


Problems:
---------
Add action modal dialog always loading with previous data    [x]
The sequence number of new added item (may not be a problem) [ ]


